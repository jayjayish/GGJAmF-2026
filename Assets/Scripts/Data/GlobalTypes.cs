
namespace Data
{
    public static class GlobalTypes
    {
        public enum SceneName : byte
        {
            MainMenu,
            Hub,
            UI,
        }

        public enum Color : byte
        {
            Red,
            Orange,
            Yellow,
            Cyan,
            Purple
        }
    }
}
